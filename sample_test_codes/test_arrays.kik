// Sample Array Code
import "io.kik";

int kik() {
    int numbers[5]= {4, 1, 5, 2, 3};

    output("Numbers are: ");
    for int i = 0; i < 5;: i = i + 1: {
        output(numbers[i]);
        output(" ");
    }
    return 0;
}