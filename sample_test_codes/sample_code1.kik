// Check whether it's a prime number 
import "io.kik";
import "string.kik";

void func(int number, int num2) {
    // null
}

int kik(){

	int number;

	output("Enter the number: ");
	input(number);
    func(number, number);
    //func2(number);
    strlen("hello");
    string s = "kik code";
    string s2 = "kik code2";

    for int i = 2; i < number;: i=i+1: {
        if number % i != 0 : {
            continue;
        }
        else: {
            output("It is not a prime number.\n");
			return 0;
		}
    }
	output("It is a prime number.\n");

    return 0;
}
